# 使い方ガイド(はじめての方向け)

このアプリを初めて触る方向けの「使い方」の説明です。ビルド方法や技術的な話は書いていません(それは [`WINDOWSでのテスト手順.md`](./WINDOWSでのテスト手順.md) を見てください)。ここでは「アプリはもう用意されている(.exeファイルを受け取った/自分でビルドし終えた)」という前提で、実際にどう使うかだけをまとめます。

## これは何をするアプリ?

PeerCastの配信を見ている人(視聴者)が、視聴ソフト(pcwmpやPCRPlayerなど)の画面から絵文字のボタンをポチッと押すと、配信者の画面にその絵文字がふわっとアニメーションで浮かび上がる、という「配信リアクション」の仕組みです。

アプリは2つに分かれています。

- **配信者アプリ**(`ReaCast.exe`): 配信する人が使います。届いたリアクションを画面にアニメーション表示します
- **視聴者アプリ**(`ReaTap.exe`): 配信を見る人が使います。pcwmpの画面のそばに、絵文字を押せる小さなバーを出します

この2つは「中継サーバー」という裏方を通じてつながっています。中継サーバーの用意・アドレスの設定は、配信者アプリ・視聴者アプリを配ってくれた人(運営している人)がすでに済ませてくれているはずなので、ふつうに使う分には意識しなくて大丈夫です。

## 動作環境・インストール方法

**Windows 10(2018年4月の大型更新「バージョン1803」以降)または Windows 11 が必要です。** それより古いWindows 10や、Windows 7/8/8.1では動作しません(このアプリが使っている表示の仕組み「WebView2」が入っていないためです)。ご自身のWindowsのバージョンは、「設定」→「システム」→「詳細情報」の「バージョン」欄で確認できます。

なお、Windows 7/8/8.1については「何か1つ部品を追加インストールすれば動くようにならないか」を調べましたが、2023年1月以降Microsoft・Tauri(このアプリの土台)の両方が同時期にサポートを打ち切っており、現実的に動かす方法が無いことを確認済みです。今後の対応予定もありません。

「WebView2」は上記のバージョン以降のWindowsであれば、Windows自体にもとから組み込まれているか、Windows Updateを通じて自動で入っています。そのため、**普段からWindows Updateを適用しているPCであれば、特別な準備なしにそのまま使えます。** 古いPCなどで万が一「WebView2 Runtime」が見つからない旨のエラーが出た場合のみ、[Microsoft公式のWebView2ダウンロードページ](https://developer.microsoft.com/microsoft-edge/webview2/)から「Evergreen Bootstrapper」をダウンロードして実行し、インストールしてください。

インストール(使い始めるまでの手順)はとても簡単です。

1. 配布された `ReaCast.exe`(配信者用)または `ReaTap.exe`(視聴者用)を受け取り、デスクトップなど好きな場所に置く
2. ダブルクリックで起動する。これだけで使い始められます

**このアプリは「インストーラ」形式ではなく、exeファイル単体で動く形で配布されています。** Start Menuへの登録やアンインストール操作は不要です(使うのをやめたい時は、そのexeファイルを削除するだけでOKです)。設定ファイルはexeとは別の場所(Windowsのユーザーごとの設定フォルダ)に保存されるので、exeファイル自体を別フォルダに移動しても設定は引き継がれます。

初回起動時にWindowsが「発行元を確認できません」と警告してくることがありますが、これはコード署名(有料の証明書)をしていないためで、動作自体には問題ありません。詳しくは下の「困ったときは」を参照してください。

## 自動アップデートについて

配信者アプリ・視聴者アプリのどちらも、起動するたびに新しいバージョンが公開されていないか自動で確認します(インターネット接続が必要です)。新しいバージョンがあった場合だけ、次のようなダイアログが出ます。

> 新しいバージョン v1.0.1 が利用可能です。今すぐ更新しますか?

- 「OK」を押すと、アプリが一旦自動的に終了し、裏側で新しいバージョンへの差し替えが行われたあと、自動的に再起動します。特に何か操作する必要はありません(インストーラのような画面も出ません)
- 「キャンセル」を押す、またはこのダイアログを閉じると、何も起きません。そのまま今のバージョンを使い続けられます。次回起動時にまた同じ確認が出ます
- 新しいバージョンが無い場合は、このダイアログ自体が出ません(黙って通常通り起動します)

万一更新に失敗した場合は、エラーメッセージとともに「お手数ですがGitHubのReleasesページから手動でダウンロードしてください」という案内が出ます。その場合は配布元(運営している人)から最新のexeを受け取って、今のexeと入れ替えてください。

## 全体の流れ(ざっくり)

1. 配信者がアプリを起動すると「合言葉」が自動で決まる(例: `ao3`)
2. 配信者がその合言葉を、いつも通りの方法(配信中の告知やSNSなど)で視聴者に伝える
3. 視聴者がアプリを起動して合言葉を入力すると、pcwmpの画面にリアクションバーが出る
4. 視聴者がバーの絵文字を押すと、配信者の画面にその絵文字がアニメーションで表示される

以下、配信者向け・視聴者向けに分けて説明します。

---

## 配信者向けの使い方

`ReaCast.exe` をダブルクリックすると、「コントロールパネル」という小さいウィンドウが起動します。これを配信中は開いたままにしておいてください(最小化はOKです)。

### 合言葉を確認・変更する

起動すると自動的にランダムな合言葉が入力欄に入っています。そのまま使ってもいいですし、好きな文字列に変えたい場合は入力欄を書き換えて「この合言葉にする」を押してください(英数字で始まる3〜32文字。ハイフン・アンダースコアも使えます。大文字小文字は区別しません)。「コピー」ボタンでコピーできるので、これを視聴者に伝えてください。

上の「接続中」「未接続」の表示が「接続中」になっていれば、中継サーバーとちゃんとつながっています。「未接続」のままの場合は、中継サーバーが起動しているか、コントロールパネルに表示されている中継サーバーのアドレスが正しいかを確認してください。

「視聴者数」には、今この合言葉で接続している人数が表示されます。

### 表示するモニターを選ぶ

複数モニターを使っている場合、「アニメーションを表示するモニター」欄から、リアクションを表示したいモニターを選べます。既定は「メインモニターに従う」です。配信で映しているモニターにだけ出したい、という場合にここで指定してください。

### 絵文字の大きさを変える

「絵文字の大きさ」のスライダーで、30%〜250%の範囲で表示サイズを調整できます(既定100%)。大きすぎる・小さすぎると感じたら動かしてみてください。動かすとすぐにプレビューに反映されます。「既定に戻す」でワンクリックで100%に戻せます。ここで決めた大きさは、次にアプリを起動した時も引き継がれます。

### 特定のリアクションを受け付けない(ON/OFF)

荒らし対策や、特定のリアクションだけ表示したくない場合、コントロールパネルの「受け付けるリアクション」欄で、リアクションごとにON/OFFを切り替えられます。チップ(丸いボタン)をクリックすると、その場でOFF(薄く表示)になります。もう一度クリックすればONに戻ります。

OFFにしたリアクションは、視聴者アプリ側では引き続きボタンとして押せます(視聴者には「送れなくなった」ことは伝わりません)が、配信者アプリ側の画面には表示されなくなります。設定は自動で保存され、次回起動時も引き継がれます。

### 実際に届くか確認する(テスト送信)

視聴者がまだ誰もいない状態でも、コントロールパネルの下の方に並んでいる絵文字ボタンを押すと、実際に視聴者から送られてきたのと同じようにテスト表示できます。配信画面の見た目を事前に確認したい時に使ってください。

### OBSでリアクションだけを表示したい場合

デスクトップを映さず、決まったものだけを配信している場合、OBSの「ウィンドウキャプチャ」でこのアプリのオーバーレイを直接映そうとすると、透明にしたい部分にデスクトップが映り込んでしまうことがあります。そういう時のために、OBSの「ブラウザ」ソースから直接読み込める表示口を用意してあります。

1. 配信者アプリ(コントロールパネル)を起動したままにしておく
2. OBSのシーンに「ブラウザ」ソースを追加する
3. URLに `http://127.0.0.1:18772/` を入力する(「ローカルファイル」のチェックは入れない)
4. 幅・高さは配信キャンバスのサイズ(例: 1920×1080)に合わせる
5. 「OK」を押せば完了。あとはリアクションが届くたびにこの上に絵文字が表示されます

このURLは配信者アプリを動かしているPC自身からしかアクセスできません(外部には公開されないので安全です)。絵文字の大きさを変えたい時は、OBS側のソースをドラッグで拡大縮小するのではなく、コントロールパネルの「絵文字の大きさ」スライダーを使ってください(こちらは通常表示・OBS表示の両方に反映されます)。

### フルスクリーンのゲームなどに重ねてリアクションを表示したい場合

リアクションのアニメーションは、他のどのウィンドウよりも手前に表示されるように作られています(ゲームなどを「常に最前面」にしていても、その上にリアクションが重なって見えるように定期的に前面へ出し直しています)。

ただし1つだけ例外があります。ゲームが**DirectXの「排他的フルスクリーン」モード**で動いている場合、OSの仕組み上、そのゲームより手前には他のどんなソフト(このアプリに限らず、Discordのオーバーレイやゲーム配信ソフトのFPS表示なども含めて)も一切表示できません。これはこのアプリ特有の制限ではなく、Windows自体の仕様によるものです。

もしゲーム画面にリアクションが重ならない場合は、そのゲームの画面設定で「フルスクリーン」ではなく「**ウィンドウモード(ボーダーレス)**」または「**フルスクリーン(ウィンドウ)**」に変更してみてください。この設定であれば見た目はフルスクリーンのままで、リアクションも問題なく重ねて表示できます。

---

## 視聴者向けの使い方

`ReaTap.exe` をダブルクリックすると、「配信者一覧」という管理ウィンドウが起動します。ここで配信者を「登録」してリアクションバーを出す、という流れになります。**複数の配信者を同時に登録して、それぞれ独立したリアクションバーを同時に出すこともできます**(見ている配信を切り替える度に繋ぎ直す必要はありません)。

### 配信者を登録する・接続する

画面上部の「配信者を追加」フォームに入力します。

1. **合言葉**: 配信者から教えてもらった文字列を入力します(必須)
2. **配信者名(任意)**: 空欄でOKです。同じPCでPeerCastを複数同時に開いていて、pcwmp/PCRPlayerのウィンドウが複数ある場合にだけ入力してください。ここに配信者側のPeerCastの名前(タイトルの一部)を入れておくと、似た名前の別ウィンドウに誤って紐付いてしまうのを防げます。普段1つしか開かない場合は気にしなくて大丈夫です
3. **表示名(任意)**: 下の一覧でその配信者を見分けるための、自分用のメモ書きです(例: 「○○さんの配信」)。空欄なら合言葉がそのまま表示されます

入力したら「接続する」を押してください。

- 緑色で成功と出れば、その配信者が「接続中・登録済みの配信者」欄にカードとして追加され、自動的にリアクションバー(絵文字ボタンだけが並ぶ小さな透明なウィンドウ)が出てきます
- 赤色でエラーが出た場合は、合言葉が間違っているか、配信者アプリ側が起動していない可能性があります。合言葉を確認してもう一度試してください

一度登録した配信者は一覧に残り続けるので、次回以降は毎回合言葉を打ち直す必要はありません。一覧のカードから「接続する」を押すだけで、そのリアクションバーがまた表示されます。アプリを起動した時、前回接続していた配信者があれば自動的に再接続してリアクションバーを出します。

新しく配信者を追加すると、その配信者の詳細設定(表示位置・行数・追従/最前面・ホットキー)には、下で説明する「新規追加時の詳細設定ひな形」の内容が自動的にコピーされます。よく使う設定を先にひな形側で決めておけば、配信者を追加するたびに一から設定し直す手間が省けます。

### リアクションバーの位置を自由に動かす・表示/非表示を切り替える

合言葉を入力して配信者を追加した直後など、その配信者のpcwmp/PCRPlayerのウィンドウがまだ見つかっていない間は、リアクションバーは**既定では表示されません**(以前のバージョンでは、行き場のない状態のバーがぽつんと画面に残ってしまっていましたが、その問題を解消しました)。対象のウィンドウが見つかり次第、自動的にその横にリアクションバーが表示されます。

リアクションバーの余白部分(ボタンとボタンの間・周囲のふち)をマウスでつまむと、バーを画面上の好きな位置にドラッグして動かせます。対象が見つからない状態でとりあえずバーを単独で表示しておきたい場合や、自動配置された位置が気に入らない場合に使ってください。

対象が見つかっているかどうかに関わらず常にそのバーを表示しておきたい場合は、配信者一覧の各カードにある「**ボタン表示**」ボタンを押してください(押すと「ボタン非表示」に表示が変わり、常時表示モードになります)。もう一度押せば既定の動作(対象が見つかるまで隠す)に戻ります。

### 配信者一覧の操作

「接続中・登録済みの配信者」欄には、登録した配信者ごとにカードが並びます。それぞれのカードでできること:

- **接続する / 切断する**: そのリアクションバーの表示・非表示を切り替えます(登録自体は消えません)
- **詳細設定 / 詳細設定を閉じる**: そのカードの下に、この配信者専用の設定パネルを開閉します(内容は次項)
- **ボタン表示 / ボタン非表示**: 対象ウィンドウが見つかっていなくてもリアクションバーを常時表示するかどうかを切り替えます(詳しくは前項)
- **削除**: 登録そのものを一覧から消します(接続中なら自動的に切断してから削除します)

カードには、そのpcwmp/PCRPlayerウィンドウが実際に見つかっているかどうかのステータスも表示されます。見つからない場合は詳細設定の中の手動選択機能を使ってください。

### 新規追加時の詳細設定ひな形(デフォルト値)

「配信者を追加」フォームの下にある「新規追加時の詳細設定ひな形(デフォルト値)を編集」を開くと、これから新しく配信者を追加した時に使われる、詳細設定の初期値をあらかじめ決めておけます。設定できる内容は、次項の「配信者ごとの詳細設定」とほぼ同じです(表示位置・縦に並べる行数・追従/最前面・ホットキーのひな形)。

ここで決めた内容は、この後「配信者を追加」した時にだけ自動的にコピーされます。**既に追加済みの配信者の設定には一切影響しません**(あとから変えたい場合は、その配信者のカードの「詳細設定」から個別に変更してください)。

ホットキーのひな形については、新規追加のタイミングで、その時点で他の接続中の配信者と重複していないものだけが自動的にコピーされます。重複するものは未設定のままになるので、追加後に個別に設定し直してください。

### 配信者ごとの詳細設定

各カードの「詳細設定」を開くと、その配信者のリアクションバーだけに効く設定を変更できます(他の配信者には影響しません)。

- **配信者名・表示名の編集**: あとから追加・変更できます
- **表示位置**: 次の5通りから選べます
  - **対象の右(縦並び・既定)**: pcwmpの右側に縦に並べて表示。映像には重なりません。並べる行数は「縦に並べる行数」で変更できます(既定10行。それを超えると自動で右にもう1列増えます)
  - **コメント欄の上(重ねて表示)**: 映像に重ねつつ、下端から少し余白(既定60px)を空けて表示。この余白はコメント入力欄を隠さないように調整できます
  - **対象の下(重ねない)**: pcwmpのすぐ下に表示
  - **対象の上(重ねない)**: pcwmpのすぐ上に表示
  - **対象の下端に重ねて表示(旧仕様)**: 映像の下端ぎりぎりに重ねて表示
- **対象ウィンドウに自動で追従する**: チェックを外すと、pcwmpを動かしてもバーは追従しなくなります
- **常に最前面に表示する**: 外すと、他のウィンドウの後ろに回ることも許すようになります
- **対象ウィンドウの手動指定**: 自動検出がうまくいかない場合、「候補一覧から選ぶ」で表示されたウィンドウ一覧から直接選ぶか、「対象ウィンドウを手動選択」でクリックして指定できます。「自動検出に戻す」でこの指定を解除できます
- **ショートカットキー(ホットキー)一覧**: この配信者向けリアクションのキー割り当て(次項)

### 表示するリアクションを選ぶ・並べ替える(全配信者共通)

管理ウィンドウ下部の「表示するリアクション・並び順」欄は、**すべての配信者のリアクションバーに共通の設定**です(配信者ごとには変えられません)。

- チェックを外すと、そのリアクションはどのバーにも表示されなくなります
- 「↑」「↓」ボタンで表示順を入れ替えられます

設定は自動で保存され、次回起動時も引き継がれます。今後リアクションが追加されても、まだ並べ替えていない新しいものは自動的に一覧の最後に追加されるので、設定が壊れる心配はありません。

### ホットキー(ショートカットキー)でリアクションを送る

クリックの代わりに、キーボードのキー(組み合わせ可)でリアクションを送れるようにできます。**この割り当ては、他のアプリ(ゲームなど)を操作している最中でも反応します。** 既定では何も割り当てられていないので、使いたい人だけ設定してください。**ホットキーは配信者ごとに別々に割り当てます**(各カードの詳細設定内の一覧から設定します)。同時に複数の配信者に接続している場合、キーを押すとそのキーを割り当てたカードの配信者だけにリアクションが飛びます。

1. 目的の配信者カードの「詳細設定」を開き、割り当てたいリアクションの行の「設定」ボタンを押す(「キーを押してください」と表示されます)
2. 割り当てたいキー(例: `F9`、または `Ctrl+Shift+F1` のような組み合わせ)を押す
3. 表示が押したキーに変わり、リアクションバーの該当アイコンの下にも小さくキー名が表示されます(どのキーがどのリアクションか、バーを見ればすぐ分かります)

すでに(同じ配信者・別の配信者を問わず)他のリアクションに割り当て済みのキーを選ぼうとするとエラーが出て上書きされません。「解除」ボタンで割り当てを外せます。数秒キーが押されないと自動的にキャンセルされます(「取消」ボタンでいつでも手動キャンセルもできます)。

### pcwmpと一緒に使う

pcwmp(またはPCRPlayer)で配信を開くと、数秒以内にリアクションバーがその画面のそばに自動で表示されます。あとはバーの絵文字を押すだけで、配信者の画面にリアクションが飛びます。連打すると絵文字が大きくなっていくので、押しすぎ・盛り上がり具合が伝わりやすくなっています(ただし0.5秒に1回までの制限があります)。

自動で出てこない場合は、上記の「配信者ごとの詳細設定」内の手動選択機能か、下の「困ったときは」を見てください。

### タスクトレイメニュー

配信者一覧ウィンドウを閉じても(×ボタンでは終了せず)、タスクトレイ(画面右下の通知領域)にアイコンが残り、リアクションバーはそのまま表示され続けます。

- **左クリック**: 配信者一覧ウィンドウを表示します
- **右クリック→「配信者一覧を開く」**: 同上
- **右クリック→「配信者一覧を隠す」**: 管理ウィンドウだけを隠します(リアクションバーは表示されたままです)
- **右クリック→「終了(すべてのリアクション表示を閉じる)」**: アプリ全体を終了し、すべてのリアクションバーを閉じます

---

## 用意されているリアクション一覧

現在、次の29種類が用意されています。

😂笑 / 👏拍手 / 💖ハート / 😮驚き / 🔥熱い / 😭泣 / 🎉GG / 💀死んだ / 🥺お願い / 草(w)(緑色の文字で表示されるオリジナルリアクション。押すたびに「草」か「w」がランダムに出ます) / 👍ナイス / 😱ヒヤッと / 💪がんばれ / 😴眠い / 🎯的中 / 🤪おどけ / 🤔考え中 / 🆗OK / 🍌バナナ / 🐸カエル / 🥭マンゴー / 🍑もも / 🍻乾杯 / ⏱計測中 / 🕔時間 / ⬆⬇➡⬅方向

それぞれ絵文字の雰囲気に合わせた専用のアニメーションが付いています(拍手はパンパン弾む、炎はチラつく、🐸はぴょんと跳ねる、など)。

---

## 困ったときは

**視聴者アプリで「接続を確認できませんでした」と出る**
合言葉が正しいか、配信者アプリ(コントロールパネル)がちゃんと起動しているか、配信者アプリ側が「接続中」表示になっているかを確認してください。

**pcwmpを開いてもリアクションバーが自動で出てこない**
配信者一覧ウィンドウ(タスクトレイ左クリック、または右クリック→「配信者一覧を開く」)を開き、該当の配信者カードの「詳細設定」から、「候補一覧から選ぶ」で表示されたウィンドウ一覧からpcwmpを直接選んでください。それでも見つからない場合は「対象ウィンドウを手動選択」で、pcwmpのウィンドウを直接クリックして指定する方法もあります。同じPCでPeerCastを複数開いている場合は、配信者の登録時に「配信者名」を入力しておくと、正しいウィンドウにだけ紐付きやすくなります。

**アプリ起動時にWindowsが「発行元を確認できません」と警告してくる**
コード署名(有料の証明書)をしていないためで、動作自体には問題ありません。「詳細情報」→「実行」で進めば起動できます。

**リアクションを押しても配信者側に反映が遅い/反応しないことがある**
連打対策として、リアクションは種類にかかわらず全体で0.5秒に1回までしか送信できない仕組みになっています。それより短い間隔で押しても間引かれます。

**OBSでデスクトップが映り込んでしまう**
「配信者向けの使い方」の「OBSでリアクションだけを表示したい場合」を参照して、OBSの「ブラウザ」ソース方式に切り替えてみてください。

上記で解決しない場合や、その他気になる挙動があれば、その時の状況(何をしたらどうなったか)を教えてください。
