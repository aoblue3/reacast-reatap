# Windowsでのテスト手順(初めての方向け・Tauri版)

このプロトタイプを、あなたのWindows PCで実際に動かして試すための手順です。上から順番にやってください。

**この手順は軽量版(Tauri版、`broadcaster-app-tauri` / `viewer-app-tauri`)向けです。** 実行ファイルのサイズが数MBと小さく、これから使う場合はこちらをおすすめします。

## 全体の流れ

1. 必要な道具(Node.js、Rust)をインストールする(まだ入っていなければ)
2. このフォルダ一式をWindows PCに置く
3. 「配信者アプリ」をビルドして起動し、合言葉を確認する
4. 「視聴者アプリ」をビルドして起動し、さっきの合言葉を入力する
5. pcwmpを開いて、リアクションバーが出るか確認する
6. リアクションボタンを押して、配信者アプリの画面に絵文字が出るか確認する
7. (OBSでデスクトップを映さず配信する場合)OBSに「ブラウザ」ソースを追加して、リアクションだけが表示されるか確認する

配信者アプリと視聴者アプリを、最初は**同じ1台のPC上**で試すのが一番簡単です(2台用意しなくてOKです)。

---

## 1. 必要な道具をインストールする

### Node.js

コマンドプロンプト(またはPowerShell)を開いて

```
node -v
```

と打ってみて、`v20` や `v22` のようなバージョンが表示されればOKです。入っていなければ、[https://nodejs.org/](https://nodejs.org/) から「LTS」と書かれている方をダウンロードしてインストールしてください(次へ次へで進めるだけでOKです)。

### Rust

このアプリはRustという言語の部分を含んでいて、ビルド(組み立て)する時にパソコン側でコンパイル(変換)する必要があります。以下の手順でインストールしてください。

1. [https://www.rust-lang.org/ja/tools/install](https://www.rust-lang.org/ja/tools/install) を開く
2. 「rustup-init.exe」をダウンロードして実行する
3. 途中で「Visual Studio C++ Build tools」が無いという趣旨のメッセージが出た場合は、案内に従って [Visual Studio Build Tools](https://visualstudio.microsoft.com/ja/visual-cpp-build-tools/) をインストールしてください(インストール時に「**C++ によるデスクトップ開発**」にチェックを入れる)。これはRustのコードをWindows向けに変換するために必要です
4. インストールが終わったら、コマンドプロンプトを開き直して

```
rustc --version
```

と打ち、バージョンが表示されればOKです。

この作業は最初の1回だけで大丈夫です(以後アプリを再ビルドする時もこの道具をそのまま使えます)。

## 2. 中継サーバーを起動する

まずは自分のPCの中だけで動作確認するので、中継サーバーも同じPCで動かします。

1. 配布したフォルダの中の `relay-server` フォルダを開く
2. その場所でコマンドプロンプトを開く(フォルダのアドレスバーに `cmd` と入力してEnterを押すと、そのフォルダでコマンドプロンプトが開きます)
3. 次のコマンドを順番に実行する

```
npm install
node server.js
```

`[relay] listening on ws://0.0.0.0:39200` のような表示が出たままになれば起動成功です。このウィンドウは**閉じずに**そのままにしておいてください。

## 3. 配信者アプリをビルドして起動する

1. `broadcaster-app-tauri` フォルダでコマンドプロンプトを開く
2. 次を実行する

```
set REACTION_RELAY_HOST=127.0.0.1
set REACTION_RELAY_PORT=39200
npm install
npm run build
```

初回はRust側のコンパイルが走るため、数分〜10分程度かかることがあります(2回目以降はもっと速くなります)。終わると `broadcaster-app-tauri\src-tauri\target\release\ReaCast.exe` ができています。**インストーラではなく、このファイル1つをダブルクリックすればそのまま起動できます**(インストール作業は不要です)。配布するときもこのexeファイル1つだけ渡せば大丈夫です。

**注意:** `set REACTION_RELAY_HOST=...` は、そのexeを**作る時(ビルド時)だけ**指定すれば十分です。一度ビルドしてしまえば、そのアドレスはexeファイルの中に焼き込まれるので、後で普通にダブルクリックして起動するだけで(コマンドプロンプトを開かなくても)正しいアドレスに繋がります。逆に言うと、アドレスを変更したい場合は `set` をやり直してから必ず `npm run build` をもう一度実行してください(exeファイルをただ再配置しても中身は変わりません)。

初回はWindowsが「発行元を確認できません」といった警告(SmartScreen)を出すことがあります。これはコード署名(有料の証明書)をしていないためで、動作自体には問題ありません。「詳細情報」→「実行」で進めます。

起動すると「コントロールパネル」という小さいウィンドウが出ます。

**接続方式を「合言葉」に変更しました。** 以前は暗号化された長い接続コード(90文字程度)を視聴者にコピペしてもらう方式でしたが、手入力しづらいという要望を受けて、配信者が決めた短い「合言葉」(英数字で始まる3〜32文字。ハイフン・アンダースコア可、大文字小文字は区別なし)を中継サーバーに登録し、視聴者はその合言葉だけを入力して参加する方式に変更しました。起動すると自動的にランダムな合言葉が生成されて入力欄に入っています。そのままでも使えますし、「この合言葉にする」ボタンで好きな文字列(合言葉)にいつでも変更できます。入力欄の右の「コピー」ボタンでコピーして、この文字列を視聴者に伝えてください。

「未接続」のままの場合は、2.の中継サーバーのウィンドウがまだ開いたままか(閉じてしまうと繋がりません)、コントロールパネルの「中継サーバー:」欄に表示されているアドレスが2.で起動したものと一致しているかを確認してください。

**表示モニターを選べるようにしました。** コントロールパネルの「アニメーションを表示するモニター」欄から、リアクションのアニメーションを表示するモニターを選べます。既定は「メインモニターに従う」で、これまで通りWindowsのメインモニターに表示されます。複数モニター環境で「配信を映しているモニターにだけ出したい」といった場合に、ここで特定のモニターを指定してください。

**絵文字の大きさを調整できるようにしました。** コントロールパネルの「絵文字の大きさ」欄のスライダーで、30%〜250%の範囲で細かく調整できます(既定100%)。大きすぎる・小さすぎると感じたら、動かした瞬間にプレビュー用の%表示が変わり、少し経つと実際の表示にも反映されます(スライダーから手を離す前でも反映されます)。「既定に戻す」で100%にワンクリックで戻せます。ここで選んだ大きさは、通常のオーバーレイ表示だけでなく、後述するOBSの「ブラウザ」ソース側にも同じように反映されます(OBS側だけ違う大きさにしたい、という場合は今のところ非対応です)。設定は自動的に保存され、次回起動時も同じ大きさで始まります。

## 4. 視聴者アプリをビルドして起動する

1. `viewer-app-tauri` フォルダでコマンドプロンプトを開く
2. 次を実行する

```
set REACTION_RELAY_HOST=127.0.0.1
set REACTION_RELAY_PORT=39200
npm install
npm run build
```

**注意:** 合言葉方式への変更にともない、視聴者アプリも中継サーバーのアドレスをビルド時に埋め込む仕組みになりました。そのため、3.の配信者アプリと同様に`set REACTION_RELAY_HOST`/`REACTION_RELAY_PORT`をビルド前に指定してください(以前は配信者アプリだけで良かったのが、視聴者アプリでも必要になった点にご注意ください)。同じPCで両方試す場合は、上の例と同じ値で構いません。

終わると `viewer-app-tauri\src-tauri\target\release\ReaTap.exe` ができています。**インストーラではなく、このファイル1つをダブルクリックすればそのまま起動できます**(インストール作業は不要です)。配布するときもこのexeファイル1つだけ渡せば大丈夫です。

初回はWindowsが「発行元を確認できません」といった警告(SmartScreen)を出すことがあります。これはコード署名(有料の証明書)をしていないためで、動作自体には問題ありません。「詳細情報」→「実行」で進めます。

**視聴者アプリの作りについて(今回、複数配信者の同時接続に対応するため作りを変更しました)。** 起動すると「配信者一覧」という管理ウィンドウ(合言葉の登録・各種設定。枠あり・不透明な普通のウィンドウ)が常に1つ出ます。そこで配信者を登録して「接続する」を押すたびに、その配信者専用の「リアクションバー」ウィンドウ(絵文字アイコンだけを配信画面に重ねて表示する、枠なし・透明のオーバーレイ)が**別ウィンドウとして**新しく作られます。複数の配信者に同時接続すると、それぞれに対応するリアクションバーが同時に何枚も表示される形になります(以前のバージョンは1つのウィンドウの見た目を切り替える方式で、同時に1配信者にしか繋げませんでした)。

起動すると「配信者一覧」ウィンドウの上部に「配信者を追加」フォームがあるので、3.で確認した合言葉を入力して「接続する」を押してください。押すと、実際に配信者の部屋に繋がるかをその場で確認し、画面に結果が表示されます。中継サーバーのアドレスを配信者から別途指定された場合のみ、「詳細設定」欄を開いてホスト・ポートを上書きしてください(通常は変更不要です)。

- 緑色でメッセージが出れば成功です。**その配信者のカードが一覧に追加され、自動的にリアクションアイコンだけの透明なバーが表示されます**
- 赤色で「接続を確認できませんでした: ...」と出た場合は失敗です(例: 中継サーバーが起動していない、アドレスが間違っている、合言葉が違う、など)。フォームの入力内容はそのまま残るので、原因を直してからもう一度「接続する」を押し直してください

**注意:** 一度登録に成功した配信者は一覧に残り続け、次回起動時は前回接続していた配信者に自動的に再接続します(合言葉を毎回入れ直す必要はありません)。リアクションバーのウィンドウは**アイコン以外何も描画しません**(背景・枠・文字などは一切出ません)。位置や見た目の設定を変えたい場合は、**配信者一覧ウィンドウの該当カードの「詳細設定」を開いてください**(下の「リアクション表示の設定について」も参照)。管理ウィンドウを閉じても(×ボタンでは終了せず)アプリ自体・リアクションバーは動き続けます。管理ウィンドウを再表示したい時は、タスクトレイのアイコンを**左クリック**してください。

## 5. pcwmp(またはPCRPlayerなどの互換ソフト)で試す

pcwmpを起動して、いつも見ている配信(またはテスト用の適当な配信)を開いてください。数秒以内に、リアクションアイコンが自動的にpcwmpの映像に重なって表示されるはずです。(pcwmp本体ではなく`PCRPlayer.exe`/`PCRPlayer64.exe`を使っている場合も自動検出の対象に含めてあります。それ以外の視聴ソフトを使っている場合は、後述の「候補一覧から選ぶ」で直接指定してください。)

**リアクション表示の設定について。** リアクションバーの表示はアイコンだけを描画する透明なオーバーレイになっています。背景・枠・検出状況の文字などは一切表示されません。そのため、表示位置や各種設定は**すべて配信者一覧ウィンドウの、各配信者カードの「詳細設定」欄にまとめてあります**(複数の配信者に同時接続している場合、この設定は配信者ごとに別々に持てます。ある配信者は「対象の右」、別の配信者は「コメント欄の上」、といった使い分けも可能です)。設定を変えると、開いているリアクションバーにその場ですぐ反映されます。

「表示位置」からは、次の5通りを選べます。

- **対象の右(縦並び・既定)**: pcwmpの右側に、アイコンを縦に並べて表示します。映像には重なりません。1列に並べる行数は詳細設定の「縦に並べる行数」で変更できます(既定10行)。アイコンの数がその行数を超えた場合は、自動で右方向にもう1列作って折り返します
- **コメント欄の上(重ねて表示)**: pcwmpの映像に重ねつつ、下端から少し余白を空けた高さに表示します。この余白(下の「下からの余白」で調整できます。既定60px)を、配信ソフト側のコメント入力欄の高さに合わせて調整することで、コメント欄を隠さずに映像の下部にアイコンだけが浮いているような見た目を狙っています。配信ソフトのウィンドウサイズやレイアウトによって最適な余白は変わるので、実際に見ながら数値を調整してみてください
- **対象の下(重ねない)**: pcwmpのすぐ下にアイコン列を表示。映像に重なりません
- **対象の上(重ねない)**: pcwmpのすぐ上にアイコン列を表示
- **対象の下端に重ねて表示(旧仕様)**: 映像の下端ぎりぎりに重ねて表示する、以前の既定の配置

**既定は「対象の右(縦並び)」です。** 映像に重ならない配置なので、まずはこのままで試してみてください。

pcwmpが画面いっぱいに最大化されていて指定の位置に置けない場合は、画面内に収まるように自動で位置を補正します。

**もう1つ、複数モニター環境向けの修正があります。** 以前は、pcwmp/PCRPlayerをモニターをまたいで別の画面に移動させた時、リアクション表示側が元のモニターの範囲に留まろうとしてしまい、うまく追従できないことがありました。今回、画面の範囲を「全モニターをまとめた範囲」として計算するように直したので、モニターをまたいでの移動にも追従できるはずです。これは複数モニター環境でのみ確認できる修正のため、私の開発環境(モニター1枚)では確認できていません。実機での確認結果を教えてください。

詳細設定の「対象ウィンドウに自動で追従する」のチェックを外せば、自動での追従を止められます(ただしリアクション表示中はウィンドウの枠が無いため、その場合は最後に計算された位置に固定されたままになります)。「常に最前面に表示する」のチェックを外せば、他のウィンドウの後ろに回ってよいという設定にもできます。

**もう1つ、地味ですが重要な修正があります。** ディスプレイの表示スケール設定(Windowsの「拡大縮小」設定。125%や150%などにしている場合)によっては、以前のバージョンではpcwmp/PCRPlayerとの位置・サイズの計算にズレが生じ、アニメーションが画面端で見切れてしまうことがあった不具合を見つけて直しました(表示スケールが100%の環境では元々問題が出ません)。この修正はWindowsの拡大縮小機能に関するものなので、私の開発環境(Linux)では計算ロジックの確認まではできましたが、実機での最終確認はできていません。125%/150%等の拡大縮小環境をお使いの場合は、リアクションバーやオーバーレイの位置・サイズがずれていないか確認してみてください。

**オーナーウィンドウ化について(実機で効果を確認済み)。** 以前「だめもとで」実験的に追加した、pcwmpを検出するたびにこのウィンドウをWindowsの「オーナーウィンドウ」という仕組みでpcwmp側に紐付ける処理(SetWindowLongPtr + GWLP_HWNDPARENT)が、pcwmpを最大化した状態でも隠れずに手前に表示されることを実機で確認していただけました。これにより、今回から**重ねる配置を既定に戻せる**ようになっています。特別な操作は不要で、これまで通り使うだけで自動的に働きます。

自動検出がうまくいかない場合、配信者一覧ウィンドウ(タスクトレイのアイコンを左クリック)で該当の配信者カードの「詳細設定」を開くと、次の2つの手動指定方法があります。

**方法1: 候補一覧から選ぶ(推奨)** 詳細設定の「候補一覧から選ぶ」欄にある「一覧を更新」を押すと、今動いているウィンドウの一覧が表示されます。この中からpcwmpのウィンドウをクリックして選んでください。クラス名やクリック操作に頼らず直接指定するので、自動検出やクリック選択がうまくいかない場合でも確実です。一覧にpcwmpらしきものが出てこない場合は、pcwmpが実際に起動しているか確認した上で「一覧を更新」を押し直してください。

**方法2: クリックして選ぶ(代替手段)** 「対象ウィンドウを手動選択」ボタンを押すと、視聴者アプリのウィンドウ自体が**一時的に画面から消えます**(これは正常な動作です。自分自身のウィンドウが「選択したウィンドウ」として誤検出されないようにするための仕組みです)。その間にpcwmpのウィンドウをクリックしてください。最大15秒待っても見つからない場合は「タイムアウトしました」というメッセージとともに自動的に元のウィンドウが再び表示されます。

**「一覧を更新」を押しても「取得中...」のまま返ってこない・「対象ウィンドウを手動選択」を押しても何も起きない、という不具合について:** 根本原因が分かりました。これらの処理はRust側で「重い処理は自動的に裏スレッドで動く」という誤った前提でコードを書いてしまっており、実際には**アプリ全体の画面更新を担当するメインスレッドをそのまま止めてしまっていました**。指示自体は出ていても、それを実際に画面へ反映する処理が同じ止まったスレッドの順番待ちになっていたため、見た目上は何も起きていないように見えていた、という仕組みです。今回、これらの処理をすべて正しく裏スレッドで動くように修正しました。この修正はWindows実機のスレッド挙動に関するものなので、私の開発環境(Linux)では見た目の再現・修正の確認ともにできましたが、実機での最終確認はできていません。直っていないようであれば、その旨を教えてください。

方法1・2のどちらでもうまくいかない場合は、その旨と、その時pcwmpを実際に起動していたかどうかを教えてください。

それでも見え方に問題がある場合は、詳細設定の「表示位置」を他のものに変えて試してみてください。それでも改善しないようであれば、その時の配置設定と、どんな風に見えている(見えていない)かを教えてください。

## 6. リアクションを送ってみる

リアクションバーの絵文字を押してみてください。配信者アプリ側のメイン画面に、絵文字がふわっと出てくれば成功です。連打すると絵文字が大きくなっていくのも確認してみてください。配信者アプリのコントロールパネルにあるテスト送信ボタンでも、同じように見た目を確認できます。

**リアクションの種類を増やし、それぞれに専用のアニメーションを付けました。** 拍手ならパンパンと弾む、炎ならチラつく、泣き顔なら震える、といったように、絵文字ごとに違う動き・演出(拍手の効果線、ハートの粒、炎の火の粉、紙吹雪など)が付きます。追加した主なリアクションは次の通りです。

**画面上での動き方(モーション)にもバリエーションを付けました。** 以前は「下から上に上がってくる」動き一辺倒でしたが、絵文字の性格に合わせて動きの種類を変えています。

- 下から上に上昇(既定・多くのリアクションで使用)
- 上から下にゆっくり沈む(😭泣き顔がうなだれる、😴眠気で舟をこぐように下がる、など)
- 画面端から勢いよく飛び込んでくる(😮驚きは不意打ちで視界に入ってくるイメージ、🎯的中はダーツのように命中するイメージ)
- 左右に大きくジグザグに揺れながら上昇する、一番躍動感のある動き(🎉GG専用)
- 突然大写しで現れてから通常サイズに落ち着く、ジャンプスケア的な出現(😱ヒヤッと)
- その場でくずおれるように少し沈む(💀死んだ)

エフェクト(拍手の効果線やハートの粒など)は残しつつ、動きそのものがその絵文字らしくなるようにしています。

- 🥺(お願い)
- ゲーム実況向けの実用的なリアクション: 👍(ナイス)、😱(ヒヤッと)、💪(がんばれ)、😴(眠い)、🎯(的中)
- **草**: ネットスラングの「草(w)」を、絵文字ではなく緑色の文字として表示するオリジナルのリアクションです

視聴者アプリのリアクションバーにも同じ絵文字が並びます(「対象の右」モードでは2列に折り返して表示されます)。

**連打対策の間隔を0.5秒に1回に変更しました。** 以前は絵文字ごとに0.25秒間隔でしたが、ご要望通り「0.5秒に1回」に、かつ絵文字の種類に関係なく全体で共通の間隔になるようにしました(ある絵文字を送った直後は、別の絵文字であっても0.5秒経つまで送信できません)。

**リアクションを14種類追加しました。** 🤪(おどけ)、🤔(考え中)、🆗(OK)、🍌(バナナ)、🐸(カエル)、🥭(マンゴー)、🍑(もも)、🍻(乾杯)、⏱(計測中)、🕔(時間)、⬆⬇➡⬅(方向)です。それぞれ専用のアニメーションを付けてあります(🐸は跳ねるように動く、⬆⬇➡⬅はその方向へまっすぐ飛ぶ、🍌はくるくる回転する、など)。テスト中に、矢印(⬆⬇➡⬅)がフォントの都合で見えなくなる(黒い文字として描画され、透明な背景に紛れて実質見えなくなる)不具合を見つけて直したので、直っているか確認してみてください。

**既存リアクションも細かく直しました。**

- 🥺(お願い)の周りに出ていたキラキラのエフェクトを削除しました
- ❤(黒く表示されてしまうことがあった)を💖に変更しました
- 草は、表示のたびに「草」と「w」をランダムで1文字だけ表示するようにし、線が細く見づらいとのことだったので太字にしました(どちらも緑色のままです)
- 🎉(GG)のアニメーションで、絵文字がいつも右を向いていた問題を直し、左右ランダムに向くようにしました

**アニメーション全体を見直し、OBS上で早めに見切れてしまう問題を直しました。** 中央から発生して上に飛んでいくような動きのものは、画面の端(上下)に到達する前に確実にフェードアウトし終えるよう、移動距離を自動的に画面の高さに合わせて調整するようにしました。特にOBSの「ブラウザ」ソースの表示エリアを配信キャンバスより小さめに設定している場合に効果があるはずです。まだ切れて見える場合は、その時の表示位置設定と画面サイズを教えてください。

## 6.5. ホットキー(ショートカットキー)でリアクションを送る

視聴者アプリの各リアクションに、クリックの代わりに使えるホットキー(グローバルショートカット)を割り当てられるようにしました。**割り当ては他のアプリを操作中でも反応します**(pcwmp/PCRPlayerやゲームの画面が最前面にあっても、キーを押せばそのままリアクションが送られます)。既定では何も割り当てられていないので、使いたい場合は設定してください。

設定は、配信者一覧ウィンドウの該当配信者カードの「詳細設定」内、ショートカットキー一覧から行います(ホットキーは配信者ごとに別々に持てるようになりました。詳しくは「6.10. 視聴者側: 配信者プロファイルの保存・複数配信者への同時接続」を参照)。

1. 割り当てたいリアクションの行にある「設定」ボタンを押す(ボタンが「取消」に変わり、「キーを押してください」と表示されます)
2. 割り当てたいキー(組み合わせも可。例: Ctrl+Shift+F1)を押す
3. 表示が押したキーに変わり、リアクションバーの該当アイコンの下にも小さくキー名が表示されるようになります(どのキーがどのリアクションか、バーを見ればひと目で分かるようにしてあります)

既に別のリアクションに割り当て済みのキーを選ぼうとした場合はエラーメッセージが出て上書きされません。「解除」ボタンで割り当てを外せます。うまくキーを検出できない場合は自動的に数秒でキャンセルされ、別のキーで試すよう案内が出ます(「取消」ボタンでもいつでも手動でキャンセルできます)。

## 6.6. 表示するリアクションの選択・並べ替え

リアクションの種類が増えてきたため、配信者一覧ウィンドウ下部の「表示するリアクション・並び順」欄(すべての配信者のリアクションバーに共通の設定です)で、視聴者アプリのバーに表示するリアクションを選んだり、並び順を変えたりできるようにしました。

- チェックを外すと、そのリアクションはバーに表示されなくなります(ホットキーは外しても引き続き使えます)
- 「↑」「↓」ボタンで表示順を入れ替えられます

設定は自動的に保存され、次回起動時にも引き継がれます。今後さらにリアクションが追加された場合も、まだ並び順を設定していない新しいリアクションは自動的に一覧の末尾に追加される仕組みにしてあるので、設定が壊れる心配はありません。

## 6.7. PCRPlayerのポップアップ・右クリックメニューへの誤追従を修正

PCRPlayerでレスの上にカーソルを合わせた時に出るスレッドのポップアップや、画面を右クリックした時に出るメニューに、リアクション表示がつられて移動してしまう不具合を直しました。原因は、対象ウィンドウを探す処理が、同じPCRPlayerのプロセスから出る小さなポップアップ/メニューのウィンドウを誤って「本体」として掴んでしまうことがあったためです。今回、複数の候補が見つかった場合は最も面積が大きいウィンドウ(=本体である可能性が高いもの)を選ぶように直しました。実機でポップアップ表示時・右クリックメニュー表示時の挙動を確認してみてください。

## 6.8. リアクションが他のウィンドウの裏に隠れて見えなくなる問題への対応

「リアクションがどこかのウィンドウの裏に回ってしまい見えなくなる」という報告があったため、配信者アプリのオーバーレイ(リアクションが表示される透明な全画面ウィンドウ)について対策を追加しました。

原因として考えられるのは、Windowsの「常に最前面」の仕組み上、後から別のアプリ(ゲームや他の常時最前面系のツールなど)が自分自身を最前面化すると、そちらが最前面グループの中で「後勝ち」してしまい、こちらのオーバーレイがその裏に回ってしまうことがある、というものです。視聴者アプリのリアクションバー側では対象ウィンドウへの追従処理のたびに最前面を取り直す保険が既に入っていましたが、配信者アプリのオーバーレイ側にはこの保険が入っていなかったため、同じ考え方で次の2つの対策を追加しました。

- 2秒おきに、オーバーレイの「最前面」指定を自動的に取り直す
- リアクションが実際に届いて表示される瞬間にも、念のため最前面を取り直す

特別な操作は不要で、これまで通り使うだけで自動的に働きます。実機で、他のアプリ(特にゲームやDiscordのオーバーレイなど、自分自身も常に最前面系の表示を行うアプリ)と一緒に使っている時に、リアクションがちゃんと最前面に出るようになったか確認してみてください。それでも裏に回ってしまう場合は、その時どんなアプリ・ウィンドウが手前にあったかを教えてください。

## 6.9. 配信者側: 受け付けるリアクションのON/OFF

配信者アプリのコントロールパネルに「受け付けるリアクション」欄を追加しました。荒らし対策や、特定のリアクションだけ表示したくない場合、リアクションごとにチップ(丸いボタン)をクリックしてON/OFFを切り替えられます。OFFにしたリアクションは、視聴者側では引き続きボタンとして押せます(視聴者には拒否されたことは伝わりません)が、配信者アプリの画面には表示されなくなります。設定は自動保存され、次回起動時も引き継がれます。今のところ配信者アプリ側だけの設定で、視聴者アプリの表示/非表示チェックとは連動していません。実機でXvfb相当の見た目(チップの見え方・トグル操作)を確認してみてください。

## 6.10. 視聴者側: 配信者プロファイルの保存・複数配信者への同時接続

これまで視聴者アプリは同時に1つの配信者にしか接続できませんでしたが、今回から**複数の配信者に同時接続し、それぞれ独立したリアクションバーを同時に表示できる**ようにしました。登録した配信者(合言葉・配信者名・表示名の組)は「プロファイル」として保存され、配信者一覧ウィンドウのカードから接続・切断・削除ができます。表示位置やホットキーなどの詳細設定も配信者ごとに個別に持てます。詳しい操作は上の「4. 視聴者アプリをビルドして起動する」「5. pcwmp(またはPCRPlayerなどの互換ソフト)で試す」の書き直した部分を参照してください。複数の配信を同時に見ながら、それぞれに別々のリアクションを送れるか実機で確認してみてください。

## 6.11. 視聴者側: PeerCast多重起動時の「配信者名」による絞り込み

同じPCでPeerCast(pcwmp/PCRPlayer)を複数同時に開いている場合、これまでは自動検出がどのウィンドウに紐付くか制御できず、意図しないウィンドウにリアクションバーが追従してしまうことがありました。今回、配信者の登録時に任意で「配信者名」を入力できるようにし、指定した場合はその名前を含むタイトルのウィンドウにしか自動検出・自動追従しない(フォールバックで別のウィンドウを掴まない)ようにしました。空欄のままなら以前と同じ挙動です。PeerCastを複数開いた状態での絞り込みの効き目を、実機で確認してみてください。

## 6.12. 視聴者側: リアクションバーの右クリックメニューを無効化

リアクションバー(絵文字ボタンが並ぶ透明なオーバーレイ)の上で右クリックすると、WebView既定の「戻る」「再読み込み」等のコンテキストメニューが出てしまい、配信画面の上に不要なメニューが浮いてしまう不具合を直しました。リアクションバー上でのみ右クリックメニューを無効化しています(配信者一覧ウィンドウの合言葉入力欄などでは、右クリックからの貼り付けが引き続き使えるよう、そちらは変更していません)。

## 7. OBSにリアクションだけを表示する(ブラウザソース)

デスクトップを映さず、決められたものだけを配信する設定にしている場合、OBSの「ウィンドウキャプチャ」や「ゲームキャプチャ」で配信者アプリのオーバーレイ(透明な全画面ウィンドウ)を直接キャプチャしようとすると、透明な部分がうまく抜けずデスクトップが映り込んでしまうことがあります。これはWindowsのウィンドウキャプチャ関連の仕組みと、タスクバー非表示・透明ウィンドウの組み合わせでよく起きる相性問題で、ウィンドウキャプチャ・ゲームキャプチャのどちらでも解決しないことがあります。

そこで、配信者アプリに**OBSの「ブラウザ」ソース向けの表示口**を追加しました。世の中の配信オーバーレイツール(アラート表示など)と同じ方式で、OBS側から直接Webページとして読み込んでもらう形です。OBSの「ブラウザ」ソースはこの手の透明背景を正しく扱えるので、デスクトップが映り込む心配がありません。

**設定手順:**

1. 配信者アプリ(コントロールパネル)を起動したままにしておく(このアプリが起動している間だけ、下のURLが使えます)
2. OBSのシーンに「ブラウザ」ソースを追加する
3. URLに次を入力する(ローカルファイルのチェックは**入れない**でください)

```
http://127.0.0.1:18772/
```

4. 幅・高さは、配信のキャンバスサイズ(例: 1920x1080)に合わせておくのがおすすめです
5. 「OK」を押せば追加完了です。あとはリアクションが届くたびに、このブラウザソースの上にふわっと絵文字が表示されます

**確認のしかた:** 配信者アプリのコントロールパネルにある「動作確認用テスト送信」の絵文字ボタンを押してみてください。OBS側のブラウザソースにも同じ絵文字が出れば成功です。

**絵文字が大きすぎる/小さすぎると感じたら:** OBS側のソース自体をドラッグで拡大縮小すると、取り込み範囲(表示エリア)ごと縮んでしまいます。取り込み範囲はそのままに絵文字だけを小さくしたい場合は、代わりにコントロールパネルの「絵文字の大きさ」スライダーを使ってください。こちらで調整した大きさは、通常のオーバーレイ表示とOBSのブラウザソースの両方に同じように反映されます。

**補足:**

- 従来通りの透明な全画面オーバーレイウィンドウ(Windowsのデスクトップ上に浮かぶもの)も、これまで通りバックグラウンドで動き続けます。OBSのブラウザソースを使う場合、そちらのウィンドウはOBSに映す必要はありません(クリックを透過するので邪魔にはなりませんが、自分の画面上には見えたままになります)
- このURL(`http://127.0.0.1:18772/`)は配信者アプリを起動しているPC自身からしかアクセスできません(外部には一切公開されないので、他の人がこのURLを開いても繋がりません)
- OBS側のブラウザソースは、配信者アプリを再起動した後も、シーンを開き直すか「更新」ボタンを押せば自動的に再接続します
- OBSの「ソースを非表示にした時にソースをシャットダウンする」のチェックは外したままにしておいてください(入れると、シーン切り替え中にリアクションを取りこぼすことがあります)

## 過去バージョン対策について

ある程度完成してユーザーに配った後、古いバージョンのアプリがいつまでも使われ続けると、後で仕組みを変えた時に古い方が変な繋がり方をしてしまう心配があると思います。そこで、配信者アプリ・視聴者アプリ・中継サーバーの間でやり取りする最初のメッセージに「プロトコルバージョン」という番号を付けるようにしました。中継サーバー側でこの番号が古すぎる接続を拒否する仕組みなので、実質的に「配信者アプリと視聴者アプリは、中継サーバーが要求する最低バージョン以上でないと繋がらない」という制限になっています。

今回はこの番号を新設した段階なので、今回配る分同士であれば普通に繋がります。今後さらに仕組みを変える必要が出てきた時は、この番号を上げることで、その時点より古いアプリ(=今回配ったものを含む)からの接続を中継サーバー側で「アプリのバージョンが古いため接続できません。最新版のアプリに更新してください。」というメッセージで弾けるようになります。弾かれた場合、変な動作をするのではなく、はっきりエラーとして表示されるので安全側に倒せているはずです。

## アプリを終了する

配信者アプリは、コントロールパネルの×ボタンを押せば(オーバーレイ含めて)アプリ全体が終了します。視聴者アプリは、**タスクトレイのアイコンを右クリックして「終了(すべてのリアクション表示を閉じる)」を選べば、配信者一覧ウィンドウ・すべてのリアクションバーを含めて確実にアプリ全体が終了します**。配信者一覧ウィンドウの×ボタンは、ウィンドウを隠すだけでアプリ自体・リアクションバーは終了しません(タスクトレイに常駐したままになります)。**リアクションバーは、意図的に枠(×ボタン含む)を消してあるため画面上に×ボタンが出ません。** アプリを完全に終了したい場合は、タスクトレイの「終了」を使ってください。

また、以前のバージョンでは、リアクションバーやオーバーレイのウィンドウが起動時にOSの入力フォーカスを奪ってしまい、その結果pcwmp/PCRPlayer側がフォーカスを失って最小化・タスクバーに落ちてしまうことがありました。これらのウィンドウは最初からフォーカスを持たないように直したので、起きなくなっているはずです。まだ発生するようであれば、どのタイミングで(起動直後/接続直後/リアクションバーが出た瞬間、など)発生するか教えてください。

---

## うまくいかない時に確認すること

- 中継サーバーのウィンドウでエラーが出ていないか
- 配信者アプリのコントロールパネルが「接続中」になっているか(「未接続」のままなら中継サーバーに繋がっていません。アドレス設定を見直してください)
- pcwmpのウィンドウタイトルが特殊で自動検出に失敗する場合は、手動選択を使う
- Windowsのファイアウォールが警告を出したら「アクセスを許可する」を選ぶ
- 視聴者アプリをダブルクリックしても画面に何も出ない場合、タスクマネージャーで既に起動済みでないか確認してください(2個目以降は自動的に無視される仕様です)。配信者一覧ウィンドウが見当たらない場合は、タスクトレイのアイコンを左クリック(または右クリック→「配信者一覧を開く」)してみてください

### `npm run build` の途中でエラーが出る場合

- `link.exe` や `MSVC` に関するエラーが出る場合は、1.の「Visual Studio Build Tools」(C++によるデスクトップ開発)が正しくインストールされているか確認してください
- `rustc`や`cargo`が見つからないというエラーが出る場合は、Rustのインストール後にコマンドプロンプトを一度閉じて開き直してみてください(PATHの反映のため)
- それでも解決しない場合は、エラーメッセージの内容を教えてもらえれば一緒に確認します

### アプリの起動時に何もウィンドウが出ない・すぐ落ちる場合

- WebView2という部品が必要ですが、Windows 10(2021年以降の更新を適用済み)・Windows 11には標準で入っています。古いWindows 10の場合は [Microsoft Edge WebView2 ランタイム](https://developer.microsoft.com/microsoft-edge/webview2/) を別途インストールしてください

## 次のステップ(自分のPCでの確認が終わったら)

- 中継サーバーを実際にインターネット上のどこか(VPS等)に置いて、離れた場所にいる視聴者からも繋がるか試す
- 配信者アプリ・視聴者アプリの両方を、`REACTION_RELAY_HOST`/`REACTION_RELAY_PORT`をそのサーバーのアドレスに向けて再度ビルドし、視聴者アプリと合言葉を配る
- 実際の配信・実際のゲーム画面でオーバーレイがちゃんと表示され続けるか確認する

ここまで確認できたら、また教えてください。うまくいかない箇所があれば、その時の状況(どのステップで、何が起きたか)を教えてもらえれば一緒に原因を調べます。
