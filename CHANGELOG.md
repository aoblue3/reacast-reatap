# 更新履歴

## v1.3.2

v1.3.1公開後の新機能・整理です。

### 新機能

- **短文リアクションを追加: 乙(おつかれ)**: 押すたびに「乙ー」「おつかれー」「お疲れ様でした！」「ほなまた～」「おつです」「乙でした」「おつかれさまー」「また見てね」「バイバーイ」の9パターンからランダムで1つを選んで表示します(それなｗ等と同じく白文字・右から左に流れるタイプ)。
- **ReaTap: 「配信者一覧」画面の表示リアクション並び替えを強化**: 従来の↑↓ボタンに加えて、①つまみ(⠿)をマウスでドラッグ&ドロップして好きな位置に挿入する、②何番目に入れるかを数字欄に直接入力する、の2つの方法で並び替えられるようにしました。遠くの位置へまとめて動かしたい時は数字入力、近くで細かく調整したい時はドラッグが便利です。

### 整理

- **ReaCast: コントロールパネルの絵文字一覧を属性ごとに並べ替え**: 「表情・リアクション系」「食べ物系」「動物・生き物系」「短文系」「マイナス系」の5グループにまとめました(テスト送信ボタン・受け付けるリアクション・連打で大きくしないリアクションの3箇所すべてに反映されます)。あわせて「草(w)」も短文系グループへ移動しました。ReaTap側の表示順は、既に保存されている並び順(reactionOrder)がそのまま優先されるため、この並び替えによる影響はありません(新しく追加した🙇乙だけが末尾に追加されます)。

## v1.3.1

v1.3.0公開後、実際に使ってみて見つかった不具合の修正です。新機能はありません。

(前回「v1.3.0fix」というバージョン表記で配布しましたが、自動アップデートの
バージョン比較処理が「1.3.0fix」の末尾の"fix"を数値として解釈できず、
実質v1.3.0のまま(パッチ番号0)と誤認識してしまい、自動アップデートが
「更新なし」と判定されて動かない不具合がありました。数字だけのバージョン
番号(v1.3.1)に付け直すことで、次回以降は正しく自動アップデートが働きます。
コード自体の修正内容はv1.3.0fixと同じです。)

### 修正

- **ReaCast: 「範囲指定」で範囲を選び終えても選択用ウィンドウが消えず、キャンセルも効かなくなる不具合を修正**: 前回(v1.3.0)、選択用ウィンドウが表示されないままアプリごと応答なしになる致命的なバグを修正しましたが、その後の検証で、選択自体は正常に完了する(スライダーにもきちんと反映される)ものの、選択用ウィンドウ自身を閉じる処理だけが常に失敗し、ウィンドウが残り続けてしまう別の不具合が見つかりました。原因は、Tauriのセキュリティ機構(権限設定)で「JS側からウィンドウを閉じる」操作の許可自体が抜けていたことで、閉じようとするたびに権限エラーになり、それを静かに無視してしまっていたためでした。必要な許可を追加し、選択後・キャンセル・Escキーのいずれでも確実にウィンドウが閉じるようにしました。
- **ReaTap: 連打時のちらつきは直ったが、pcwmp/PCRPlayerのコメント欄にフォーカスがある状態で絵文字を押すと、そのフォーカスが戻らなくなっていた不具合を修正**: 前回、連打時のちらつき対策として「クリックのたびに配信ソフト側へフォーカスを戻す」処理自体を撤去しましたが、これによりコメント入力中に絵文字を押すとコメント欄からフォーカスが外れたままになってしまっていました。今回、「クリックした時点で配信ソフト側が本当に手前(フォアグラウンド)だったかどうか」を判定できるようにし、手前だった場合(=コメント欄を使っていた最中だった場合)だけフォーカスを戻すようにしました。他の作業をしている最中に押した場合は何もしないため、連打時のちらつきも再発しません。
- **ReaTap: リアクションバーが「対象に追従していない」状態のとき、moveカーソルになる場所なのにドラッグしても動かせない箇所があった不具合を修正**: ドラッグ移動の処理を特定の要素だけに限定していたため、見た目のカーソルと実際に動かせる範囲にズレが生じていました。ウィンドウ全体(ボタンの上を除く)のどこをつまんでも確実にドラッグで動かせるように修正しました。

## v1.3.0

v1.2.9公開後の修正・追加機能です。

### 修正

- **ReaCast: 🎆花火を連発すると表示のコマ落ちが目立つ不具合を改善**: 花火は1回のリアクションで100個を超えるパーティクル(紙吹雪・輝き粒等)を発生させる演出になっており、連発すると同時に生存するパーティクル数に歯止めが無かったため、CPU/GPU使用率には出にくい形でメインスレッドの負荷が積み重なり、コマ落ちして見える原因になっていました。パーティクルの同時表示数にも上限を新設し、超えた分は古いものから即座に片付けるようにしたほか、花火1箇所あたりの個数自体も少し絞りました。
- **ReaTap: それなｗ等の短文リアクションボタンの横幅が、絵文字ボタンと揃っておらず不自然に見える不具合を修正**: 全ボタン共通で絵文字1文字分の固定幅に揃え、短文側はその幅の中に収まるよう文字を小さくして2行程度に折り返す形にしました。
- **ReaTap: リアクションボタンを連打すると、配信ウィンドウ全体がちらつく不具合が引き続き発生する問題に再対応**: 前回(v1.2.8)、ReaTap自身がクリックでOSのフォーカスを奪わないようにする対応を行いましたが、それでもなお、クリックのたびに「検出した配信ソフト側のウィンドウへ明示的にフォーカスを戻す」処理を呼び続けていたことが、逆にWindows側のフォーカス奪取防止機能によるちらつき(タイトルバー等の点滅フォールバック)を誘発していた可能性が高いと判断し、この「念のため」の処理自体を撤去しました。
- **ReaCast: 「範囲指定」ボタンを押すと、選択用ウィンドウが表示されないままリアクション表示が完全に止まり、アプリごと応答なしになる致命的な不具合を修正**: 選択用ウィンドウを作る処理がアプリのメインスレッド上で実行されてしまっており、ウィンドウの初期化中にそのままメインスレッドごと固まってしまっていたことが原因でした(選択用ウィンドウが一切表示されない・オーバーレイのリアクション表示が止まる・アプリ全体が「応答なし」になる、という報告いただいた症状すべてに一致する原因です)。他の同様のウィンドウ作成処理と同じ形に修正し、メインスレッドを塞がないようにしました。
- **ReaCast: ⚽サッカーボールのアニメーションが、消える0.5秒ほど前に不自然に停止して見える不具合に再対応**: 前回・前々回の対策でも、物理シミュレーションを打ち切るタイミングが実際に画面から消えるタイミングよりわずかに早く、その間だけ静止して見える「間」が残っていました。シミュレーションを続ける区間を、実際に消えるタイミングのほぼ直前まで伸ばし、この静止区間をほぼ無くしました。

### 新機能

- **ReaCast: 個別のリアクションごとに「連打しても大きくしない」を設定できるように**: 「連打すると絵文字が大きくなる」の全体ON/OFFとは別に、リアクションごとにチップのチェックを外すことで、そのリアクションだけ常に同じ大きさで表示できるようになりました。既定では全リアクションがチェック済み(=従来通り全て大きくなる)です。
- **ReaCast: リアクション表示の「クールタイム」を設定できるように**: 同じリアクションが短い間隔で連続して届いても、指定した秒数より短い間隔になる分は表示しないようにできます。0.5秒(既定)〜5.0秒の範囲でスライダーから調整できます。表示率(確率で間引く機能)とは別軸の、間隔で間引く機能です。

## v1.2.9

v1.2.8公開後の修正・追加機能です。

### 修正

- **ReaCast: それなｗ/わかるｗ等の短文リアクションが、1文字ずつ縦に並んで表示されてしまう不具合を修正**: 常に1行で横並びに表示されるよう修正しました。あわせて、文言の末尾に「ｗ」を付け、神ｗ以外は白文字に統一しました。
- **ReaTap: それなｗ等の短文リアクションのボタン文字が薄く見づらい不具合を修正**: 太字化とグロー(縁取り)を追加し、他の絵文字ボタンと並んでもはっきり見えるようにしました。

### 新機能

- **神ｗを「ゲーミングカラー」(虹色が流れ続けるグラデーション文字)に変更**: ReaCastのオーバーレイ・ReaTapのボタン・ReaCastのテスト送信ボタンのすべてで反映されます。
- **絵文字リアクションを追加: 🎆花火**: 画面のランダムな位置で、本体+近くの2地点にタイミングをずらして次々にバーストする、ド派手な花火演出です。

## v1.2.8

v1.2.7公開後の修正・追加機能です。

### 修正

- **ReaCast: 💣爆弾が着地直後、しばらく半透明に見えてしまう不具合を修正**: フェードイン(透明→不透明)にかける時間を、爆発までの待ち時間を含んだ表示時間全体と同じにしていたため、実際に着地して動きが止まった後もしばらく不透明度が上がりきらず、半透明のまま表示され続けてしまっていました。フェードインの時間を「動き終わるタイミング」基準の短い時間に切り離すことで、着地した時点でしっかり不透明に見えるようにしました(フェードアウトの尺は従来通りです)。
- **ReaTap: リアクションボタンを押すたびに配信ウィンドウが非アクティブになり、連打すると枠の色がちらつく不具合を修正**: 以前はボタンを押すたびに一瞬だけReaTap側にフォーカスが移り、それを都度配信ウィンドウへ戻す、という事後対応をしていたため、連打時にその奪い合いが目に見えるちらつきになっていました。今回、ReaTapのリアクションバー自体がクリックされてもOS側のフォーカスを一切奪わないよう構造的に変更したため、この奪い合い自体が起こらなくなりました。

### 新機能

- **ReaCast: 💣爆弾の爆発演出をより派手に強化**: 中心の閃光・二重の衝撃波・色付きの火花や破片の飛散を追加し、爆発の瞬間の見応えを強化しました。
- **短文リアクションを4種追加(それな/わかる/うまい/神)**: ニコニコ動画のコメントのように、画面右から左へ流れて消えるリアクションです。色もそれぞれ変えてあります。
- **絵文字リアクションを5種追加**: 👶(縦横無尽にハイハイ)/🐧(とことこヨチヨチ歩き)/🐶(ワン!と吠える)/🐷(ブー!と鳴く)/👊(その場に構えて集中線)。
- **ReaCast: 表示範囲をマウスのドラッグで選べる「範囲指定」ボタンを追加**: 「モニターの一部の範囲だけに表示する」欄に、ボタンを押すと選んだモニターいっぱいに専用の選択画面が開き、マウスでドラッグして範囲を指定できるようになりました。決定すると4つのスライダーの値も自動的に連動します(スライダーでの微調整はこれまで通り可能です)。

## v1.2.7

v1.2.6公開後の追加機能です。ReaCast側に3点追加しました。

### 新機能

- **ReaCast: リアクションの「表示率」を1〜100%で設定できるように**: 視聴者からのリアクションが多すぎて画面が賑やかになりすぎる、という声を受けて追加しました。既定は100%(従来通り全件表示)。低くするほど、受信したリアクション1件ごとに表示するかどうかを抽選で間引き、実際に画面に出る量を減らします(視聴者側の連打防止やミュートには一切影響しません。あくまで配信画面の見た目の調整用です)。
- **ReaCast: アニメーションを表示するモニターの、さらに一部の範囲だけに表示できるように**: 「アニメーションを表示するモニター」欄の下に、モニターの左端・上端・幅・高さを割合(%)で指定できる欄を追加しました。画面の一角にだけリアクションを出したい場合などにお使いください。
- **ReaCast: 「自分の画面には表示しない(OBS経由の配信画面にのみ表示)」を追加**: ONにすると、配信者自身のモニターにはリアクションが表示されなくなりますが、OBSの「ブラウザ」ソース連携を使っている場合、視聴者が見る配信画面には通常通り表示されます(配信者の作業の邪魔をせず、視聴者側にだけ見せたい場合向けです)。

## v1.2.6

v1.2.5公開後、実機での確認で見つかった2点の不具合修正です。

### 修正

- **ReaCast・ReaTap: タスクトレイのアイコンを左クリックすると、メニューが一瞬表示されてすぐ閉じるバグを修正**: トレイアイコンは既定で「左クリックでもメニューを開く」設定になっており、そこに今回追加した「左クリックでウィンドウを復元する」独自の処理が同時に反応してしまい、開いた直後のメニューがウィンドウにフォーカスを奪われて即座に閉じる、という不具合でした。左クリックでのメニュー表示自体を無効化し、右クリック=メニュー表示・左クリック=ウィンドウ復元、とそれぞれ単独で動くようにしました。
- **ReaCast: ⚽サッカーボールが、消える少し前に突然その場で止まって見える不具合を修正**: 前回、シミュレーションを続ける時間(budget)自体は表示時間のほぼ全域まで伸ばしましたが、実際にはボールの弾みが物理的にすぐ収まってしまい、そこから先はずっと静止したまま表示され続けていたため、根本的な解決になっていませんでした。今回は、縦方向の弾みが収まった後にその場で完全静止させる代わりに、床の上をゆっくり転がり続ける(壁に当たれば跳ね返る)ようにし、表示が終わって消える瞬間までボールが必ず動き続けるようにしました。

## v1.2.5

- **ReaTap: リアクションバーをドラッグしても表示位置を変更できなくなっていた不具合を修正**: ダブルクリックで誤ってウィンドウが最大化される不具合(v1.0.0で対応済み)を防ぐために設定していた`resizable(false)`が、副作用としてこのウィンドウ自体のドラッグ移動(data-tauri-drag-region)まで効かなくしてしまっていたのが原因でした。最大化の抑止には`maximizable(false)`だけで十分なため、そちらだけを残す形に修正し、念のためJS側からも明示的にウィンドウ移動を開始する処理を保険として追加しました。
- **ReaCast・ReaTap: 最小化すると、タスクバーの通常のアイコンではなく、右下のタスクトレイに収まるように変更**: 「_」ボタンで最小化した際、ウィンドウごと隠してタスクトレイのアイコンだけを残すようにしました(ReaCastには今回新たにタスクトレイアイコンを追加しました)。タスクトレイアイコンの左クリック、またはメニューの「開く」で元のウィンドウに戻ります。「終了」を選ぶとアプリ全体が終了します(配信者アプリの場合はオーバーレイ表示も終了します)。

### 新機能

- **ReaTap: リアクションバーの見た目(アイコンの大きさ・背景の濃さ)を設定できるように**: 配信者一覧画面に「リアクションバーの見た目」欄を追加しました(全配信者共通の設定です)。

### アニメーション強化(ReaCastオーバーレイ)

- **👏拍手: もっとパチパチと連続で叩いているように動きを強化**
- **🆗OK・💀死んだ: 画面下部だけでなく、画面のどこにでも表示されるように変更**
- **💩: 固定の演出ではなく、⚽サッカーボールと同じ重力・反発シミュレーションでバウンドするように変更(より自然な動きに)**
- **👺天狗: レーザービーム演出を廃止し、消える間際にその場で弾けて爆発するように変更**
- **🍜ラーメン: 湯気エフェクトが絵文字自身を常に追尾するように変更(以前は開始位置に固定で発生していました)**
- **⚽サッカーボール: 表示終了間際に急停止して見える不具合を修正(物理シミュレーションを表示時間いっぱいまで継続)/ 画面下から上へ勢いよく投げ込まれるパターンをより分かりやすく強化 / ボール全体の速度を引き上げ**

## v1.2.4

v1.2.3公開後、更新確認自体は成功して「ダウンロード中です」の表示まで進むのに、その後アプリが終了したまま再起動しない現象が引き続き発生していました。今回、その原因を突き止めました。

### 修正

- **ReaCast・ReaTap: 自動アップデートの差し替え処理が、PowerShellスクリプト実行のブロックによって一切進まないことがある不具合を修正**: 差し替え・再起動処理を、これまでTempフォルダに書き出したPowerShellスクリプトを実行する方式で行っていましたが、ログが1行も書き込まれないまま(＝スクリプトが実行された形跡が全く無いまま)アプリだけが終了する事例が繰り返し確認されました。「ダウンロードしたスクリプトをTempから`powershell -ExecutionPolicy Bypass`で実行する」という手順は、マルウェアの典型的な挙動とも重なるため、セキュリティソフトや組織のポリシーによってスクリプトの実行自体が(警告も出さずに)ブロックされていた可能性が高いと判断しました。PowerShellや他のスクリプトエンジンを一切使わず、Rustのコードだけで「旧バージョンの終了待ち→ファイル差し替え→再起動」を行う方式に全面的に書き換えました。
- **重要**: この修正は、ダウンロードする新しいexe自身に新しい差し替えロジックが入っている前提の仕組みです。そのため**この方式が入ったバージョン(v1.2.4)への更新1回だけは、今まで通り自動アップデートが失敗する可能性があります**(今動いている古いバージョンには、まだ新しい差し替えロジック自体が入っていないため)。v1.2.4への更新は手動でのファイル差し替え(Tempに残った`.exe.new`を目的の場所へコピー)で行ってください。**v1.2.4に到達した後の、次回以降の自動アップデートからは、この新しい仕組みが正常に働くはずです。**

## v1.2.3

v1.2.2公開後も「アップデートのダウンロードは終わったはずなのにアプリが再起動しない(落ちるだけ)」という報告が続いたため、さらに調査・対策しました。

### 修正

- **ReaCast・ReaTap: 差し替え後のアプリ再起動が、多重起動防止の仕組みと競合して失敗することがある不具合を修正(の可能性が高い問題への対策)**: 差し替えスクリプトが旧プロセスの終了を検知した直後は、アプリの多重起動防止の判定がまだ「起動中」を引きずっていることがあり、新しいアプリを起動してもすぐに(多重起動と誤判定されて)終了してしまう可能性がありました。旧プロセス終了後に少し間を置いてから差し替えるようにし、さらに起動後に実際にプロセスが生き残っているかを確認して、消えていた場合は間隔を空けて最大3回まで再試行するようにしました。(実際にはこの対策自体は効果を発揮する機会がなく、v1.2.4で判明したPowerShellブロックが真の原因でした。)
- 手動でのファイル差し替え・起動では問題なく最新版が起動できることを確認済みで、ダウンロード自体・ファイル自体には問題が無いことは切り分け済みです。

## v1.2.2

v1.2.1公開後も「アップデートのダウンロードは終わったはずなのにアプリが再起動しない(落ちるだけ)」という報告があったため、原因切り分け用の調査ログを追加しました。

### 修正

- **ReaCast・ReaTap: 差し替え・再起動を行うヘルパースクリプトに調査用ログを追加**: ダウンロード自体は成功していても、その後の「実行ファイルのコピー→再起動」処理側で何かに失敗している可能性があるため、各ステップの結果を`%TEMP%\reacast-update-<プロセスID>.log`(ReaTapは`reatap-update-<プロセスID>.log`)に記録するようにしました。同じ現象が起きた場合、このログを見れば「コピーに失敗しているのか」「再起動(Start-Process、SmartScreenによるブロックの可能性を含む)自体に失敗しているのか」を切り分けられます。

## v1.2.1

v1.2.0公開後、自動アップデート機能がうまく動かない(確認ダイアログでOKを押しても再起動されず、固まったように見える)という報告を受けての修正です。

### 修正

- **ReaCast・ReaTap: 自動アップデートのダウンロードが、通信状況によっては終わらず固まったように見えることがある不具合を修正**: 新しいexeをダウンロードする際にタイムアウトを設定していなかったため、通信が詰まった場合に無期限に待ち続けてしまい、失敗の通知も出ないまま何も起きていないように見えることがありました。ダウンロードに明示的なタイムアウト(接続15秒・全体180秒)を設定し、タイムアウトした場合はきちんとエラーメッセージが表示されるようにしました。
- **ReaCast・ReaTap: アップデートのダウンロード中、進行中なのか固まっているのか分からず不安になる点を改善**: OKを押した後、ダウンロードが終わるまでの間「ダウンロード中です。しばらくお待ちください…」という表示を出すようにしました。

## v1.2.0

テスターの方からのフィードバックを受けての修正・機能追加まとめです。

### 修正

- **連打防止をサーバー側で強化**: 中継サーバーが視聴者ごとの連打防止(レート制限)の状態を接続(WebSocket接続)単位で管理していたため、ミュートされてもアプリを繋ぎ直すだけで連打防止をすり抜けられてしまう不具合を修正しました。今後は「部屋(配信者)+接続元IPアドレス」単位で状態を管理するため、繋ぎ直してもミュートは引き継がれます。また、許容回数を10秒間20回(クライアント側の連打制限からの理論上の最速値ぴったり)から19回に減らし、タイミングのずれによる誤ミュートが起きにくいよう余裕を持たせました。
- **ReaTap: 配信者名を指定せずに接続した際、リアクションバーが点滅することがある不具合を修正**: 対象ウィンドウの検出が単発で失敗しただけで表示/非表示を切り替えてしまっていたのが原因でした。数回連続で検出に失敗した場合だけ非表示にするよう変更し、単発のブレでは表示状態を維持するようにしました。
- **ReaTap: 追従なしの状態でリアクションバーをダブルクリックすると、ウィンドウが最大化されて画面いっぱいに巨大化する不具合を修正**: バーのドラッグ移動用の領域をダブルクリックすると、OS側の「ウィンドウの最大化」動作が発動してしまっていたのが原因でした。バーウィンドウを最大化・リサイズ不可に変更し、発生しないようにしました。
- **ReaTap: 🆗(OK)の絵文字が黒く表示され、何の絵文字か分かりにくい不具合を修正**: 絵文字を表示するボタン類に色付き絵文字用フォントを明示していなかったため、環境によっては白黒の記号として表示されてしまっていました。ReaCastのオーバーレイと同様、絵文字専用フォントを優先するよう指定しました。
- **ReaCast: 新規追加したマイナスリアクション(💩👺😡👎🖕)が、既にアプリを使ったことがある環境では既定でON(チェック有り)になってしまう不具合を修正**: 「一度も設定を保存したことがない場合のみ既定値を適用する」という判定が、既に別の設定を保存済みの環境では正しく働いていなかったのが原因でした。マイナスリアクションの既定値適用ずみフラグを別途持たせ、既存の設定を壊さずに一度だけ確実に反映されるようにしました。

### 新機能

- **ReaCast: スタンプ(オーバーレイ絵文字)の透明度を設定できるように**: コントロールパネルに新しく「スタンプの透明度」スライダーを追加しました。配信画面の見た目に合わせて薄く表示したい場合に調整できます。
- **ReaCast: 連打で絵文字が大きくなる仕様をOFFにできるように**: 「連打すると絵文字が大きくなる」チェックボックスを追加しました。OFFにすると常に同じ大きさで表示されます。
- **ReaTap: リアクションボタンを押した後、PCRPのコメント入力欄へフォーカスを自動的に戻すように**: これまではボタンをクリックするとバー側にフォーカスが移ってしまい、コメント入力を続けるにはもう一度コメント欄をクリックし直す必要がありました。
- **ReaTap: 検出中のウィンドウ名から配信者名を入力できるように**: 合言葉だけで接続した配信者の詳細設定に「検出中のウィンドウ名から入力」ボタンを追加しました。今検出できているpcwmp/PCRPlayerウィンドウのタイトルをそのまま配信者名の入力欄にコピーします(絞り込み用の項目なので、必要に応じて編集してください)。
- **新しいリアクションを11種類追加**:
  - ポジティブ(既定でON): 💣爆弾(着地後にランダムなタイミングで爆発)/ 🧨爆竹(左右どちらかへ斜め上に跳ねる)/ 🍜ラーメン(湯気付き)/ 🍣寿司(画面のどこにでも出現)/ 🎂おめでとう / ⚽サッカーボール(画面の四方から飛んできて、重力・反発を伴う物理シミュレーションで壁や床にバウンドする)
  - ネガティブ(既定でOFF。使いたい場合はReaCastの「受け付けるリアクション」から個別にONにしてください): 💩(着地後に軽くバウンド)/ 👺天狗(到着した瞬間、鼻先から右方向へレーザービーム風の効果を出す)/ 😡怒り / 👎ブーイング / 🖕
- **ReaCast: 「受け付けるリアクション」一覧を、通常リアクションとマイナスリアクション(💩👺😡👎🖕)で見出しを分けて表示するように**: 一覧をひと目で見分けやすくしました。
- **自動アップデート機能**(前回の更新で追加済み): インストーラ形式に変えず、単体exe配布のままGitHub Releasesの新バージョンを確認・適用できるようになりました。詳しくはREADME.mdの「自動アップデート機能について」を参照してください。

### 調査のみ(対応なし)

- **Windows 7/8/8.1対応について**: 調査の結果、Microsoft・Tauri側の両方でWindows 7/8/8.1のサポートが既に打ち切られており(WebView2は2023年1月以降のバージョンでWindows 7/8/8.1向けの提供が終了、Tauri側も最新のビルド環境では動作しません)、現実的に対応する方法が無いことが分かりました。今後もWindows 10(バージョン1803)以降専用とします。
- **古いバージョンからの接続拒否**: 既に意図通り機能していることを確認済みです(中継サーバーがprotocolVersionで古いアプリを弾く仕組み)。今回のリリースでは追加の対応は行っていません。

## v1.1.0

- ReaCast・ReaTapの両方に、単体exe配布のままの自動アップデート機能を追加(GitHub Releasesの最新版を起動時に確認し、確認ダイアログでOKを押した場合のみ自動的に差し替え・再起動)。
- ビルドCI(build-release.yml)でバージョン情報の埋め込み・SHA256チェックサムの生成に対応。

## v1.0.0

- GitHub Actionsによる自動ビルド・GitHub Release公開の仕組みを導入。
- オープンソースとして公開(MITライセンス)。
- ReaTapのリアクションバーをドラッグ移動・自動非表示可能に。
- 新規追加時の詳細設定ひな形機能を追加。
- ReaCastオーバーレイの最前面取り直しをリアクション送出時にバースト強化。
